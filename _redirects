# ─────────────────────────────────────────────────────────────
# Fairbairn Inspection Services — legacy URL redirects
# Old WordPress site → new static site on Cloudflare Pages
# All 301s (permanent) to pass along existing SEO link equity.
# Cloudflare Pages reads this file automatically from the repo root.
# ─────────────────────────────────────────────────────────────

# ── Core pages ──
/blog-2/                                            /blog.html 301
/blog-2                                             /blog.html 301
/sample-home-inspection-report/                     /sample-report.html 301
/sample-home-inspection-report                      /sample-report.html 301
/contact/                                           /index.html#contact 301
/contact                                            /index.html#contact 301

# ── Geo-targeted "Home Inspector [City]" landing pages ──
# Rebuilt as real dedicated city pages (Sept 2026) — each one now lives at
# its own path (e.g. /home-inspector-vancouver-bc-v5k/index.html) and is
# served directly. IMPORTANT: no /home-inspector-* wildcard redirect here —
# Cloudflare Pages redirects always win over a matching static asset
# regardless of specificity (first-match-wins, not most-specific-wins), so a
# catch-all would silently swallow every one of these pages before the
# static file is ever served. If a NEW unbuilt geo page shows up later, add
# its exact path as its own explicit rule above this comment, or build the
# real page instead. Full list of pages now live:
#   /home-inspector-vancouver-bc-v5k/
#   /home-inspector-surrey-bc-v3r/
#   /home-inspector-burnaby-bc-v3j-v3n/
#   /home-inspector-coquitlam-bc-v3b-v3c/
#   /home-inspector-richmond-bc-v6v-v6w/
#   /home-inspector-westminster-bc-v3l/
#   /home-inspector-north-vancouver-bc-v7g-v7h/
#   /home-inspector-port-coquitlam-bc-v5k/
#   /home-inspector-maple-ridge-bc-v2w-v2x/
#   /home-inspector-langley-bc-v3a-v1m/

# ── Blog posts already migrated into blog.html (deep-link to the real section) ──
/surrey-new-roof-inspection/                        /blog.html#post-surrey-roof 301
/new-construction-home-inspection-tips/             /blog.html#post-new-construction 301

# ── Blog posts not yet migrated — send to the blog index for now ──
/worst-designed-shower-ever/                        /blog.html 301
/leaking-house-shaughnessy-vancouver-home-inspector/ /blog.html 301
/inspecting-mobile-home-surrey-featured-home-inspection/ /blog.html 301
/included-condo-inspection-bc/                      /blog.html 301

# ── Category / tag archives ──
/category/*                                         /blog.html 301
/tag/*                                               /blog.html 301

# NOTE: Any individual old blog post not explicitly listed above is NOT
# covered by a wildcard here — Cloudflare Pages only supports a trailing
# "/*" splat, not mid-path globbing, so a blind catch-all isn't reliable
# and risks swallowing real paths. See message below for how to get the
# complete list and add the remaining posts explicitly.
